<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.codetribe.polokwanetourguide">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/polokwanet"
        android:label="@string/app_name"
        android:roundIcon="@drawable/polokwanet"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

        <uses-feature
            android:glEsVersion="0x00020000"
            android:required="true" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDR2vEldTU92lhi69kZndFgYrU4OEnZnp4" />

        <activity
            android:name=".MapsActivity"
            android:parentActivityName=".Main2Activity"
            android:label="@string/title_activity_maps">

        </activity>

        <activity android:name=".Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            ></activity>

        <activity android:name=".Bakone"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".Cricket"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".ImageWild"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".LionTour"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".Mall"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".Meropa"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".Musician"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".OldPlace"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".Park"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".Peter"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".Resorts"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".Sculpture"
            android:parentActivityName=".Main2Activity">
        </activity>

        <activity android:name=".TownLodge"
            android:parentActivityName=".Main2Activity">
        </activity>
    </application>

</manifest>